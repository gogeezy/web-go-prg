apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-go-prg.fullname" . }}
  labels:
    {{- include "web-go-prg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-go-prg.selectorLabels" . | nindent 4 }}
